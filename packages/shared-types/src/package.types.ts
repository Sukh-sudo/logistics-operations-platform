export type PackageStatus = 'RECEIVED' | 'SORTED' | 'IN_CONTAINER' | 'IN_TRAILER' | 'DEPARTED' | 'ARRIVED' | 'OUT_FOR_DELIVERY' | 'DELIVERED' | 'ATTEMPTED_DELIVERY' | 'DAMAGED' | 'MISROUTED' | 'RETURNED_TO_TERMINAL';
export type PackageType = 'MAIL' | 'CONVEYABLE' | 'NON_CONVEYABLE' | 'DANGEROUS_GOODS';
export type PackageEventType = 'PACKAGE_RECEIVED' | 'PACKAGE_SORTED' | 'PACKAGE_LOADED_TO_CONTAINER' | 'PACKAGE_UNLOADED_FROM_CONTAINER' | 'PACKAGE_LOADED_TO_TRAILER' | 'PACKAGE_UNLOADED_FROM_TRAILER' | 'PACKAGE_DEPARTED' | 'PACKAGE_ARRIVED' | 'PACKAGE_OUT_FOR_DELIVERY' | 'PACKAGE_DELIVERED' | 'PACKAGE_ATTEMPTED_DELIVERY' | 'PACKAGE_DAMAGED' | 'PACKAGE_MISROUTED' | 'PACKAGE_RETURNED_TO_TERMINAL' | 'PACKAGE_LOADED_TO_LAST_MILE' | 'PACKAGE_REMOVED_FROM_LAST_MILE';
export interface CreatePackageEventDto { trackingNumber: string; eventType: PackageEventType; terminalId?: number; employeeId?: number; }
export interface PackageSnapshotDto { id: string; trackingNumber: string; packageType: PackageType; currentStatus: PackageStatus; currentTerminalId: number | null; currentContainerId: string | null; currentTrailerId: string | null; currentRouteId: string | null; currentTruckId: string | null; updatedAt: string; }
export interface PackageLocationDto { trackingNumber: string; currentStatus: PackageStatus; containerBarcode: string | null; trailerBarcode: string | null; }
export interface PackageEventDto { id: string; packageId: string; eventType: PackageEventType; terminalId: number | null; employeeId: number | null; correlationId: string; metadata: unknown | null; createdAt: string; }
